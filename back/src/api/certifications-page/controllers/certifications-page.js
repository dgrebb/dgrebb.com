'use strict';

/**
 * certifications-page controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::certifications-page.certifications-page');
