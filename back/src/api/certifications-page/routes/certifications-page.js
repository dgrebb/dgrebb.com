'use strict';

/**
 * certifications-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::certifications-page.certifications-page');
